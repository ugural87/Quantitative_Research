# Core: state_space.py
numpy>=1.24
scipy>=1.10

# Notebook: data handling and plots
pandas>=2.0
matplotlib>=3.7
jupyter>=1.0

# Optional but strongly recommended for repeated walk-forward fitting.
# The estimator runs a pure-Python Kalman filter without it; numba makes
# multi-start MLE roughly an order of magnitude faster.
numba>=0.58

# Development
pytest>=7.4
